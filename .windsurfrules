# bwa-rust 项目级 Windsurf 规则

## 项目概述

bwa-rust 是一个受 BWA/BWA-MEM 启发的 Rust 版 DNA 序列比对器，核心功能包括：
- FM 索引构建（后缀数组 + BWT）
- SMEM 超级最大精确匹配种子查找
- 带状仿射间隙 Smith-Waterman 局部对齐
- 种子链构建与过滤
- SAM 格式输出

参考实现为 bwa-0.7.19 (C 语言版)，目标是用 Rust 实现等价的 `bwa mem` 单端比对管线。

## 技术栈

- **语言**: Rust 2021 Edition, MSRV 1.70
- **CLI**: clap 4.5 (derive 模式)
- **序列化**: serde + bincode
- **并行**: rayon
- **错误处理**: anyhow (binary) + 自定义 BwaError (library)
- **基准测试**: criterion
- **分配器**: tikv-jemallocator (非 MSVC 平台)
- **文档站**: VitePress

## 代码风格与规范

1. **格式化**: 遵循 `rustfmt.toml` — 120 列宽, 4 空格缩进, Tall 参数布局
2. **Lint**: 遵循 `clippy.toml` — 认知复杂度 ≤ 30, 函数 ≤ 200 行, 参数 ≤ 8
3. **安全性**: `unsafe_code = "forbid"` — 禁止使用 unsafe 代码
4. **Commit**: 遵循 Conventional Commits (feat/fix/docs/refactor/test/perf/ci)
5. **注释与文档**: 使用中文注释，公共 API 使用 `///` 文档注释
6. **错误处理**: library 层使用 `BwaError`/`BwaResult`，binary 层使用 `anyhow::Result`

## 模块结构

```
src/
├── main.rs          # CLI 入口 (clap)
├── lib.rs           # Library 入口
├── io/              # FASTA/FASTQ/SAM 解析与输出
├── index/           # FM 索引 (sa.rs, bwt.rs, fm.rs, builder.rs)
├── align/           # 对齐算法 (seed, chain, sw, extend, candidate, mapq, pipeline)
├── util/            # DNA 编码/解码/反向互补
└── error.rs         # 自定义错误类型
```

## 领域知识

- **字母表编码**: {0:$, 1:A, 2:C, 3:G, 4:T, 5:N}, SIGMA=6
- **FM 索引格式**: 单一 `.fm` 文件 (bincode 序列化, magic=0x424D_4146_4D5F5253)
- **对齐流程**: FASTQ → 正向+反向互补 → SMEM 种子 → 链构建 → SW 扩展 → 候选去重 → SAM 输出
- **默认参数 (BWA-MEM 风格)**: match=1, mismatch=4, gap_open=6, gap_ext=1, band_width=100, min_seed_len=19

## 编码指南

1. 性能敏感代码（如 SW 对齐、SMEM 查找）优先考虑内存复用和缓存友好性
2. 使用 `SwBuffer` 等可复用缓冲区模式减少热路径上的内存分配
3. 并行处理使用 rayon 的 `par_chunks` 模式批量处理 reads
4. 新增算法模块应参考 bwa-0.7.19 的对应 C 实现，但用 Rust 惯用法重写
5. 索引格式变更需递增版本号并保持向后兼容
6. 测试数据使用 `data/` 目录下的 `toy.fa` 和 `toy_reads.fq`

## 测试与 CI

- 单元测试: `cargo test`
- 集成测试: `tests/integration.rs`
- 基准测试: `cargo bench` (criterion)
- CI 流程: fmt → clippy → test → build release
- 新功能必须附带测试，性能关键路径建议添加 criterion 基准

## 不要做的事

- 不要使用 unsafe 代码
- 不要在热路径上使用 `String` / `format!` 进行不必要的分配
- 不要修改 `.fm` 文件的 magic number 或破坏索引格式兼容性
- 不要在 library 层 (lib.rs) 使用 `println!`，应使用 `eprintln!` 或返回错误
- 不要删除或弱化现有测试
